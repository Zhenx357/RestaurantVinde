# Legacy AngularJS Site

This folder contains the archived static/AngularJS implementation of Restaurant Vinde.

- Files are read-only documentation; do not deploy or edit unless you are specifically updating the legacy reference.
- The modern Next.js application lives in `/web`.
- Any new features, fixes, or content changes should be implemented in `/web` and, eventually, through the future admin dashboard.
